import { WordSpacingPipe } from './word-spacing.pipe';

describe('WordSpacingPipe', () => {
  it('create an instance', () => {
    const pipe = new WordSpacingPipe();
    expect(pipe).toBeTruthy();
  });
});
